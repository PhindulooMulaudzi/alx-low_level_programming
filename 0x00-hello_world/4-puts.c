#include <stdio.h>

/**
 * main - Prints "Programming is like building a multilingual puzzle
 * Return: 0 if exited seccuesfully, or non-zero if failed
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
